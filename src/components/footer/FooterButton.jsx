import { Button } from "react-bootstrap";

const FooterButton = function () {
  return (
    <div>
      <Button className="text-secondary bg-black border border-secondary">
        Service Code
      </Button>
    </div>
  );
};

export default FooterButton;
